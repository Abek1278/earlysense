'use client'

import { useEffect, useMemo, useState } from 'react'
import { ArrowRight, BrainCircuit, Check, CircleCheck as CheckCircle2, ChevronLeft, ChevronRight, ClipboardCheck, Eye, Hand, HeartPulse, Menu, Move3d, ShieldCheck, Sparkles, Users, X } from 'lucide-react'

const questions = [
  ['Response to Name', 'How often does the child respond when their name is called?', 'Social Interaction'],
  ['Eye Gaze', "How often does the child look toward a person's face during interaction?", 'Eye Gaze & Joint Attention'],
  ['Eye Contact', "How often does the child look toward another person's eyes or face during interaction?", 'Eye Gaze & Joint Attention'],
  ['Gestures', 'How often does the child use gestures such as pointing, waving, reaching or showing something to communicate?', 'Communication'],
  ['Communication Attempts', 'How often does the child make attempts to communicate wants, needs or interests with other people?', 'Communication'],
  ['Joint Attention', 'How often does the child look toward an object or activity when another person points to it or directs attention toward it?', 'Eye Gaze & Joint Attention'],
  ['Social Interest', 'How often does the child show interest in interacting with other people?', 'Social Interaction'],
  ['Social Play', 'How often does the child participate in simple social play or interaction with another person?', 'Social Interaction'],
  ['Imitation', 'How often does the child imitate simple actions, gestures or sounds made by another person?', 'Imitation & Play'],
  ['Facial Expressions', 'How often does the child show different facial expressions during interaction or communication?', 'Facial Behaviour'],
  ['Head Movement', 'How often does the child turn their head toward people, sounds or objects that attract their attention?', 'Movement Behaviour'],
  ['Repetitive Movements', 'How often does the child show repeated movements or repeated actions during play or daily activities?', 'Movement Behaviour'],
  ['Repetitive Play', 'How often does the child repeatedly perform the same action with toys or objects instead of varying the activity?', 'Imitation & Play'],
  ['Body Movement & Posture', 'How often does the child show noticeable patterns in body movement or posture during activities?', 'Movement Behaviour'],
  ['Social Attention', 'How often does the child shift attention between another person and an object or activity during interaction?', 'Social Interaction'],
] as const
const options = ['Never', 'Rarely', 'Sometimes', 'Often', 'Almost Always']
const categories = {
  'Social Interaction': ['Response to name', 'Social interest', 'Social play', 'Social attention'],
  Communication: ['Gestures', 'Communication attempts'],
  'Eye Gaze & Joint Attention': ['Eye gaze', 'Eye contact', 'Joint attention'],
  'Facial Behaviour': ['Facial expressions'],
  'Imitation & Play': ['Imitation', 'Repetitive play'],
  'Movement Behaviour': ['Head movement', 'Repetitive movements', 'Body movement and posture'],
}

type Assessment = { name: string; age: string; caregiver: string; relationship: string; gender: string; answers: Record<number, string> }

const defaultData: Assessment = { name: '', age: '', caregiver: '', relationship: '', gender: '', answers: {} }

export function EarlySenseApp() {
  const [view, setView] = useState<'home' | 'info' | 'questions' | 'data'>('home')
  const [data, setData] = useState<Assessment>(defaultData)
  const [accepted, setAccepted] = useState(false)
  const [questionIndex, setQuestionIndex] = useState(0)
  const [mobileOpen, setMobileOpen] = useState(false)

  useEffect(() => {
    const saved = window.localStorage.getItem('earlySenseAssessment')
    if (saved) setData(JSON.parse(saved))
  }, [])
  useEffect(() => {
    window.localStorage.setItem('earlySenseAssessment', JSON.stringify(data))
  }, [data])

  const goHome = (section?: string) => {
    setView('home'); setMobileOpen(false)
    if (section) setTimeout(() => document.getElementById(section)?.scrollIntoView({ behavior: 'smooth' }), 50)
    else window.scrollTo({ top: 0, behavior: 'smooth' })
  }
  const start = () => { setView('info'); setMobileOpen(false); window.scrollTo({ top: 0, behavior: 'smooth' }) }
  const infoValid = data.name.trim() && data.age && data.caregiver.trim() && data.relationship && accepted
  const current = questions[questionIndex]
  const completed = Object.keys(data.answers).length
  const distribution = options.map((option) => Object.values(data.answers).filter((answer) => answer === option).length)
  const maxDistribution = Math.max(...distribution, 1)

  return <div className="min-h-screen bg-background text-foreground">
    <header className="sticky top-0 z-40 border-b border-border/70 bg-background/90 backdrop-blur-xl">
      <div className="mx-auto flex h-18 max-w-7xl items-center justify-between px-5 lg:px-8">
        <button onClick={() => goHome()} className="flex items-center gap-3 text-left" aria-label="EARLYSENSE AI home">
          <span className="flex size-10 items-center justify-center rounded-xl bg-primary text-primary-foreground shadow-sm"><BrainCircuit className="size-5" /></span>
          <span><span className="block font-mono text-sm font-bold tracking-[0.2em] text-primary">EARLYSENSE</span><span className="block text-[10px] font-medium tracking-[0.24em] text-muted-foreground">AI · RESEARCH PROTOTYPE</span></span>
        </button>
        <button className="rounded-lg p-2 lg:hidden" onClick={() => setMobileOpen(!mobileOpen)} aria-label="Toggle navigation">{mobileOpen ? <X /> : <Menu />}</button>
        <nav className={`${mobileOpen ? 'absolute left-0 right-0 top-full flex border-b bg-background p-5 shadow-lg' : 'hidden'} flex-col gap-4 lg:static lg:flex lg:flex-row lg:items-center lg:border-0 lg:bg-transparent lg:p-0 lg:shadow-none`}>
          <button onClick={() => goHome('about')} className="nav-link">About</button>
          <button onClick={() => goHome('parameters')} className="nav-link">Behavioural Parameters</button>
          <button onClick={() => goHome('why-ai')} className="nav-link">Why AI?</button>
          <button onClick={() => goHome()} className="nav-link">Home</button>
          <button onClick={start} className="button-primary">Start Assessment <ArrowRight className="size-4" /></button>
        </nav>
      </div>
    </header>

    {view === 'home' && <Home onStart={start} onExplore={() => document.getElementById('about')?.scrollIntoView({ behavior: 'smooth' })} />}
    {view !== 'home' && <AssessmentShell view={view} onHome={() => goHome()} />}
    {view === 'info' && <InfoPage data={data} setData={setData} accepted={accepted} setAccepted={setAccepted} valid={!!infoValid} onContinue={() => { setQuestionIndex(0); setView('questions'); window.scrollTo(0, 0) }} />}
    {view === 'questions' && <QuestionPage index={questionIndex} current={current} answer={data.answers[questionIndex]} setAnswer={(answer) => setData({ ...data, answers: { ...data.answers, [questionIndex]: answer } })} onPrevious={() => setQuestionIndex(Math.max(0, questionIndex - 1))} onNext={() => questionIndex === questions.length - 1 ? setView('data') : setQuestionIndex(questionIndex + 1)} />}
    {view === 'data' && <DataPage data={data} distribution={distribution} maxDistribution={maxDistribution} onRestart={() => { setData(defaultData); setAccepted(false); setView('info') }} />}
    {view === 'home' && <Footer onNavigate={goHome} />}
  </div>
}

function Home({ onStart, onExplore }: { onStart: () => void; onExplore: () => void }) {
  const parameters = [
    [Eye, 'Eye Gaze & Visual Attention', 'Observes where the child looks and how visual attention shifts during interaction.'],
    [Users, 'Eye-Contact Patterns', 'Records observations related to looking toward faces or eyes during interaction.'],
    [Sparkles, 'Facial Expressions', 'Observes visible changes in facial expressions during interaction.'],
    [Move3d, 'Head Orientation & Movement', 'Considers head turning, orientation and movement patterns.'],
    [Hand, 'Hand Gestures & Body Posture', 'Records observable gestures, posture and body movement.'],
    [ClipboardCheck, 'Repetitive Movement Patterns', 'Records repeated visible movements or repeated actions.'],
    [HeartPulse, 'Social Interaction Behaviour', 'Considers combined behavioural observations during social interaction.'],
  ] as const
  return <main>
    <section className="hero-shell"><div className="mx-auto grid max-w-7xl items-center gap-12 px-5 py-20 lg:grid-cols-[1.08fr_.92fr] lg:px-8 lg:py-28">
      <div className="animate-fade-up"><div className="eyebrow"><span className="size-2 rounded-full bg-accent" /> AI-ASSISTED BEHAVIOURAL SCREENING</div><h1 className="mt-6 font-mono text-2xl font-bold tracking-[0.18em] text-primary md:text-3xl">EARLYSENSE AI</h1><h2 className="mt-4 max-w-3xl text-balance font-serif text-4xl leading-[1.08] tracking-tight text-primary md:text-6xl">Understanding early behaviour through <em className="text-accent">structured data.</em></h2><p className="mt-7 max-w-2xl text-pretty text-lg leading-8 text-muted-foreground">EARLYSENSE AI is an academic prototype that explores how observable behavioural patterns in young children can be collected and structured as data for future AI-assisted screening support.</p><div className="mt-9 flex flex-col gap-3 sm:flex-row"><button onClick={onStart} className="button-primary px-6 py-3.5">Start Assessment <ArrowRight className="size-4" /></button><button onClick={onExplore} className="button-secondary px-6 py-3.5">Explore the Project</button></div><p className="mt-7 max-w-xl text-xs leading-5 text-muted-foreground">This prototype is intended for academic and screening-support research only. It does not provide a medical diagnosis and does not replace professional evaluation.</p></div>
      <div className="relative animate-fade-up delay-150"><div className="hero-card"><div className="flex items-center justify-between border-b border-border/70 pb-5"><div><p className="font-mono text-xs tracking-widest text-muted-foreground">DATA COLLECTION / 01</p><p className="mt-1 font-serif text-2xl text-primary">A clearer starting point.</p></div><span className="flex size-11 items-center justify-center rounded-full bg-accent/15 text-accent"><ShieldCheck className="size-5" /></span></div><div className="mt-7 grid grid-cols-2 gap-3"><MiniMetric label="Behavioural areas" value="07" /><MiniMetric label="Questions" value="15" /></div><div className="mt-6 rounded-xl bg-secondary/70 p-4"><div className="mb-3 flex items-center justify-between text-xs"><span className="font-medium text-primary">Collection pathway</span><span className="text-muted-foreground">30% milestone</span></div><div className="flex gap-1.5">{['Child info', 'Questions', 'Data'].map((x, i) => <div key={x} className="flex-1"><div className={`h-1.5 rounded-full ${i < 2 ? 'bg-accent' : 'bg-border'}`} /><p className="mt-2 text-[10px] text-muted-foreground">{x}</p></div>)}</div></div><div className="mt-6 flex items-center gap-3 text-sm text-muted-foreground"><span className="flex size-8 items-center justify-center rounded-full bg-primary text-primary-foreground"><Check className="size-4" /></span> Structured observations, ready for future research.</div></div><div className="absolute -bottom-5 -left-5 rounded-xl border border-border bg-background px-4 py-3 shadow-lg"><p className="text-[10px] font-semibold uppercase tracking-widest text-muted-foreground">Current scope</p><p className="mt-1 text-sm font-semibold text-primary">Behavioural Data only</p></div></div>
    </div></section>
    <section id="about" className="section-shell"><div className="mx-auto max-w-7xl px-5 lg:px-8"><SectionIntro label="THE PROJECT" title="Why EARLYSENSE AI?" text="Early identification of developmental concerns can help families seek appropriate professional evaluation and support. Behavioural observations can provide useful information about social interaction, communication, attention, gestures and movement." /><p className="mt-5 max-w-3xl text-muted-foreground leading-7">Our project explores a structured digital workflow for collecting these observable behavioural characteristics and preparing them as data for a future AI/ML analysis system.</p><div className="mt-12 grid gap-5 md:grid-cols-3"><FeatureCard icon={Eye} title="Observe" text="Collect structured observations about the child's behaviour." /><FeatureCard icon={ClipboardCheck} title="Structure" text="Convert caregiver observations into organized behavioural data." /><FeatureCard icon={BrainCircuit} title="Analyse" text="Prepare the collected behavioural data for future AI/ML analysis." /></div></div></section>
    <section className="bg-secondary/55"><div className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><SectionIntro label="THE WORKFLOW" title="From observation to organized data" text="A focused first milestone that makes each stage clear, calm and understandable." /><div className="mt-12 grid gap-4 md:grid-cols-4">{[['01', 'Child Information'], ['02', 'Behavioural Questions'], ['03', 'Parent/Caregiver Responses'], ['04', 'Behavioural Data']].map(([n, t], i) => <div key={n} className="process-step"><span>{n}</span><div><h3>{t}</h3>{i < 3 && <ArrowRight className="mt-4 hidden size-4 text-accent md:block" />}</div></div>)}</div><p className="mt-10 rounded-xl border border-accent/30 bg-background p-4 text-center text-sm text-muted-foreground"><strong className="text-primary">Future Development:</strong> Backend <span className="mx-2 text-accent">→</span> AI/ML Analysis <span className="mx-2 text-accent">→</span> Screening-Support Output</p></div></section>
    <section id="parameters" className="section-shell"><div className="mx-auto max-w-7xl px-5 lg:px-8"><SectionIntro label="OBSERVATION FRAMEWORK" title="Behavioural Parameters" text="The prototype collects observations across multiple behavioural areas." /><div className="mt-12 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{parameters.map(([Icon, title, text]) => <div key={title} className="parameter-card"><span className="icon-box"><Icon className="size-5" /></span><h3>{title}</h3><p>{text}</p></div>)}</div></div></section>
    <section id="why-ai" className="bg-primary text-primary-foreground"><div className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><SectionIntro dark label="THE RATIONALE" title="Why use AI if humans can observe behaviour?" text="AI is not intended to replace human expertise. The proposed system is designed to assist by structuring behavioural observations and eventually analysing patterns consistently." /><div className="mt-12 grid gap-5 lg:grid-cols-2"><Comparison title="Traditional Observation" steps={['Human observes behaviour', 'Human interprets observations', 'Human decides whether further evaluation may be needed']} /><Comparison title="Proposed AI-Assisted Approach" steps={['Parent/Caregiver observes behaviour', 'Structured behavioural data', 'Future AI/ML analysis', 'Screening-support indication', 'Professional evaluation']} /></div><p className="mt-8 text-sm text-primary-foreground/65">The current prototype focuses on behavioural data collection. AI/ML model development is planned for the next stage.</p></div></section>
    <section className="section-shell"><div className="mx-auto grid max-w-7xl gap-10 px-5 lg:grid-cols-[1fr_.7fr] lg:px-8"><div><SectionIntro label="ABOUT EARLYSENSE AI" title="A low-cost, non-invasive research prototype" text="EARLYSENSE AI is a proposed AI-assisted early behavioural screening-support system. The project explores how observable behavioural features such as eye gaze, facial expressions, head movement, gestures, body posture and repetitive movements can be converted into structured data." /><p className="mt-5 text-muted-foreground leading-7">Our current prototype demonstrates the first stages of the proposed workflow: child information collection, behavioural questioning and behavioural data organization.</p></div><div className="callout-card"><p className="eyebrow">PROJECT OBJECTIVE</p><p className="mt-4 font-serif text-2xl leading-snug text-primary">To develop a low-cost, non-invasive prototype that can collect measurable behavioural features and prepare them for future AI-assisted screening analysis.</p><div className="mt-7 border-t border-border pt-5"><p className="font-semibold text-primary">Important</p><p className="mt-2 text-sm leading-6 text-muted-foreground">EARLYSENSE AI is a project prototype for screening-support research. It does not diagnose autism and should not be used as a substitute for qualified healthcare professionals.</p></div></div></div></section>
  </main>
}

function MiniMetric({ label, value }: { label: string; value: string }) { return <div className="rounded-xl border border-border bg-background p-4"><p className="font-mono text-3xl text-primary">{value}</p><p className="mt-1 text-xs text-muted-foreground">{label}</p></div> }
function FeatureCard({ icon: Icon, title, text }: { icon: typeof Eye; title: string; text: string }) { return <div className="feature-card"><span className="icon-box"><Icon className="size-5" /></span><h3>{title}</h3><p>{text}</p></div> }
function SectionIntro({ label, title, text, dark = false }: { label: string; title: string; text: string; dark?: boolean }) { return <div className={dark ? 'text-primary-foreground' : ''}><p className={`eyebrow ${dark ? 'text-accent' : ''}`}>{label}</p><h2 className="mt-4 max-w-3xl text-balance font-serif text-4xl leading-tight md:text-5xl">{title}</h2><p className={`mt-5 max-w-2xl text-lg leading-8 ${dark ? 'text-primary-foreground/70' : 'text-muted-foreground'}`}>{text}</p></div> }
function Comparison({ title, steps }: { title: string; steps: string[] }) { return <div className="rounded-2xl border border-primary-foreground/15 bg-primary-foreground/8 p-6"><h3 className="font-mono text-sm uppercase tracking-widest text-accent">{title}</h3><div className="mt-6 flex flex-col gap-3">{steps.map((step, i) => <div key={step} className="flex items-center gap-3"><span className="flex size-7 shrink-0 items-center justify-center rounded-full bg-accent/15 font-mono text-xs text-accent">{i + 1}</span><span className="text-sm text-primary-foreground/80">{step}</span></div>)}</div></div> }

function AssessmentShell({ view, onHome }: { view: string; onHome: () => void }) { return <div className="border-b bg-secondary/40"><div className="mx-auto max-w-5xl px-5 py-8 lg:px-8"><button onClick={onHome} className="mb-7 text-sm text-muted-foreground hover:text-primary">← Back to home</button><div className="flex items-center justify-between gap-2">{[['info', '01', 'Child Information'], ['questions', '02', 'Behavioural Questions'], ['data', '03', 'Behavioural Data']].map(([key, n, label], i) => <div key={key} className={`flex items-center gap-2 ${view === key ? 'text-primary' : 'text-muted-foreground'}`}><span className={`flex size-8 items-center justify-center rounded-full font-mono text-xs ${view === key ? 'bg-accent text-accent-foreground' : 'border border-border bg-background'}`}>{view === key ? <Check className="size-4" /> : n}</span><span className="hidden text-xs font-medium sm:block">{label}</span>{i < 2 && <span className="mx-1 h-px w-5 bg-border sm:mx-4 sm:w-12" />}</div>)}</div></div></div> }

function InfoPage({ data, setData, accepted, setAccepted, valid, onContinue }: { data: Assessment; setData: (d: Assessment) => void; accepted: boolean; setAccepted: (v: boolean) => void; valid: boolean; onContinue: () => void }) { return <main className="mx-auto max-w-3xl px-5 py-12 lg:px-8 lg:py-16"><div className="mb-9"><p className="eyebrow">STEP 01 · GETTING STARTED</p><h1 className="mt-4 font-serif text-5xl text-primary">Child Information</h1><p className="mt-4 text-lg leading-8 text-muted-foreground">Enter basic information before beginning the behavioural assessment.</p></div><div className="form-card"><div className="grid gap-5 sm:grid-cols-2"><label className="field sm:col-span-2"><span>Child&apos;s Name <b>*</b></span><input value={data.name} onChange={(e) => setData({ ...data, name: e.target.value })} placeholder="Enter child's name" /></label><label className="field"><span>Child&apos;s Age <b>*</b></span><select value={data.age} onChange={(e) => setData({ ...data, age: e.target.value })}><option value="">Select age</option>{Array.from({ length: 7 }, (_, i) => `${18 + i} months`).map((age) => <option key={age}>{age}</option>)}</select></label><label className="field"><span>Gender <small>(optional)</small></span><select value={data.gender} onChange={(e) => setData({ ...data, gender: e.target.value })}><option value="">Prefer not to say</option><option>Female</option><option>Male</option><option>Other</option></select></label><label className="field sm:col-span-2"><span>Parent/Caregiver Name <b>*</b></span><input value={data.caregiver} onChange={(e) => setData({ ...data, caregiver: e.target.value })} placeholder="Enter name" /></label><label className="field sm:col-span-2"><span>Relationship to Child <b>*</b></span><select value={data.relationship} onChange={(e) => setData({ ...data, relationship: e.target.value })}><option value="">Select relationship</option>{['Parent', 'Guardian', 'Caregiver', 'Other'].map((x) => <option key={x}>{x}</option>)}</select></label></div><div className="mt-7 border-t border-border pt-6"><label className="flex cursor-pointer items-start gap-3 text-sm leading-6 text-muted-foreground"><input type="checkbox" checked={accepted} onChange={(e) => setAccepted(e.target.checked)} className="mt-1 size-4 accent-[var(--accent)]" /><span>I understand that this is an academic behavioural screening-support prototype and not a medical diagnostic tool. <b className="text-primary">*</b></span></label></div><button disabled={!valid} onClick={onContinue} className="button-primary mt-8 w-full justify-center py-3.5 disabled:cursor-not-allowed disabled:opacity-40">Continue to Behavioural Assessment <ArrowRight className="size-4" /></button></div></main> }

function QuestionPage({ index, current, answer, setAnswer, onPrevious, onNext }: { index: number; current: readonly [string, string, string]; answer?: string; setAnswer: (a: string) => void; onPrevious: () => void; onNext: () => void }) { return <main className="mx-auto max-w-3xl px-5 py-12 lg:px-8 lg:py-16"><div className="flex items-end justify-between gap-4"><div><p className="eyebrow">STEP 02 · OBSERVATION</p><h1 className="mt-4 font-serif text-5xl text-primary">Behavioural Assessment</h1></div><span className="font-mono text-sm text-muted-foreground">{index + 1} <span className="text-border">/</span> 15</span></div><p className="mt-4 text-lg leading-8 text-muted-foreground">Please answer based on the child&apos;s usual behaviour. Select the option that best represents what you normally observe.</p><div className="mt-8 h-2 rounded-full bg-secondary"><div className="h-2 rounded-full bg-accent transition-all duration-500" style={{ width: `${((index + 1) / 15) * 100}%` }} /></div><div className="question-card mt-10"><span className="rounded-full bg-accent/12 px-3 py-1 text-xs font-semibold text-accent">{current[2]}</span><h2 className="mt-7 font-serif text-3xl leading-snug text-primary">{current[1]}</h2><div className="mt-8 grid gap-3">{options.map((option) => <button key={option} onClick={() => setAnswer(option)} className={`answer-option ${answer === option ? 'selected' : ''}`}><span className={`radio-dot ${answer === option ? 'selected' : ''}`}>{answer === option && <Check className="size-3" />}</span>{option}</button>)}</div></div><div className="mt-8 flex items-center justify-between gap-3"><button onClick={onPrevious} disabled={index === 0} className="button-secondary disabled:invisible"><ChevronLeft className="size-4" /> Previous</button><button onClick={onNext} disabled={!answer} className="button-primary disabled:cursor-not-allowed disabled:opacity-40">{index === 14 ? 'Complete Assessment' : 'Next'} {index === 14 ? <CheckCircle2 className="size-4" /> : <ChevronRight className="size-4" />}</button></div></main> }

function DataPage({ data, distribution, maxDistribution, onRestart }: { data: Assessment; distribution: number[]; maxDistribution: number; onRestart: () => void }) { return <main className="mx-auto max-w-6xl px-5 py-12 lg:px-8 lg:py-16"><div className="flex flex-col justify-between gap-5 border-b border-border pb-8 md:flex-row md:items-end"><div><p className="eyebrow">STEP 03 · STRUCTURED OUTPUT</p><h1 className="mt-4 font-serif text-5xl text-primary">Behavioural Data</h1><p className="mt-4 max-w-2xl text-lg leading-8 text-muted-foreground">The behavioural responses provided during the assessment have been organized into structured data for future analysis.</p></div><div className="flex items-center gap-2 rounded-full bg-accent/12 px-4 py-2 text-sm font-medium text-accent"><CheckCircle2 className="size-4" /> Assessment Data Collection — COMPLETE</div></div><div className="mt-10 grid gap-5 md:grid-cols-4"><InfoStat label="Child Name" value={data.name} /><InfoStat label="Age" value={data.age} /><InfoStat label="Parent/Caregiver" value={data.caregiver} /><InfoStat label="Relationship" value={data.relationship} /></div><div className="mt-12 grid gap-8 lg:grid-cols-[1.25fr_.75fr]"><div><h2 className="font-serif text-3xl text-primary">Behavioural Response Summary</h2><div className="mt-5 overflow-hidden rounded-2xl border border-border bg-background"><div className="hidden grid-cols-[.7fr_1.5fr_.5fr] gap-4 border-b bg-secondary/50 px-5 py-3 text-xs font-semibold uppercase tracking-wider text-muted-foreground md:grid"><span>Behavioural Feature</span><span>Question</span><span>Response</span></div>{questions.map(([feature, question, category], i) => <div key={feature} className="grid gap-2 border-b border-border px-5 py-4 last:border-0 md:grid-cols-[.7fr_1.5fr_.5fr] md:items-center md:gap-4"><div><p className="text-xs font-semibold text-accent">{category}</p><p className="mt-1 font-medium text-primary">{feature}</p></div><p className="text-sm leading-5 text-muted-foreground">{question}</p><span className="w-fit rounded-full bg-secondary px-3 py-1 text-xs font-semibold text-primary">{data.answers[i]}</span></div>)}</div></div><div><h2 className="font-serif text-3xl text-primary">Response Distribution</h2><div className="mt-5 rounded-2xl border border-border bg-background p-6"><div className="flex h-52 items-end justify-between gap-3 border-b border-border pb-0">{distribution.map((count, i) => <div key={options[i]} className="flex h-full flex-1 flex-col items-center justify-end gap-2"><span className="font-mono text-xs text-muted-foreground">{count}</span><div className="w-full rounded-t-md bg-accent transition-all" style={{ height: `${Math.max(8, (count / maxDistribution) * 75)}%` }} /><span className="mb-[-1.4rem] mt-2 rotate-[-45deg] text-[10px] text-muted-foreground">{options[i].replace('Almost ', 'A. ')}</span></div>)}</div><p className="mt-10 text-xs leading-5 text-muted-foreground">A neutral view of response frequency. This visualization does not calculate a score or diagnosis.</p></div><h2 className="mt-12 font-serif text-3xl text-primary">Data Categories</h2><div className="mt-5 flex flex-col gap-3">{Object.entries(categories).map(([name, items]) => <div key={name} className="rounded-xl border border-border bg-background p-4"><div className="flex items-center justify-between"><p className="font-semibold text-primary">{name}</p><span className="font-mono text-xs text-muted-foreground">{items.length} features</span></div><p className="mt-2 text-xs leading-5 text-muted-foreground">{items.join(' · ')}</p></div>)}</div></div></div><div className="mt-12 rounded-2xl border border-accent/30 bg-accent/8 p-7"><div className="flex gap-4"><span className="flex size-10 shrink-0 items-center justify-center rounded-full bg-accent text-accent-foreground"><Check className="size-5" /></span><div><h2 className="font-serif text-2xl text-primary">Behavioural Data Collection Completed</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">The responses have been successfully collected and organized into behavioural categories.</p><p className="mt-5 text-sm font-semibold text-primary">Next Development Stage</p><p className="mt-1 text-sm leading-6 text-muted-foreground">The structured behavioural data will serve as the input for the planned backend and AI/ML analysis module in the next stage of the project.</p><p className="mt-5 font-mono text-xs uppercase tracking-widest text-accent">Current prototype ends here.</p></div></div></div><button onClick={onRestart} className="button-secondary mt-8">Start a new assessment</button></main> }
function InfoStat({ label, value }: { label: string; value: string }) { return <div className="rounded-xl border border-border bg-background p-4"><p className="text-xs uppercase tracking-wider text-muted-foreground">{label}</p><p className="mt-2 truncate font-medium text-primary">{value}</p></div> }
function Footer({ onNavigate }: { onNavigate: (section?: string) => void }) { return <footer className="border-t border-border bg-secondary/40"><div className="mx-auto flex max-w-7xl flex-col gap-8 px-5 py-10 md:flex-row md:items-end md:justify-between lg:px-8"><div><p className="font-mono text-sm font-bold tracking-[0.2em] text-primary">EARLYSENSE AI</p><p className="mt-2 text-sm text-muted-foreground">AI-Assisted Early Behavioural Screening Support</p></div><div className="flex flex-wrap gap-x-5 gap-y-2 text-sm text-muted-foreground"><button onClick={() => onNavigate('about')}>About</button><button onClick={() => onNavigate('parameters')}>Behavioural Parameters</button><button onClick={() => onNavigate()}>Assessment</button></div><p className="max-w-xs text-xs leading-5 text-muted-foreground">This website is an academic prototype. It is not intended to diagnose autism or replace professional medical evaluation.</p></div></footer> }

export function HomePage() { return <EarlySenseApp /> }
